{
  "schemaVersion": 1,
  "kind": "desired-fault-specifications",
  "status": "FTS specifications executed against approved f4b9135; unresolved routing and new FTS recovery specifications remain unexecuted.",
  "specs": [
    {
      "id": "ADV-FTS-repeat-insert",
      "defectId": "G2-D002",
      "target": "src/lib/db.ts",
      "fault": "Restore the pre-fix INSERT OR REPLACE write path so repeated writes leave duplicate FTS entries.",
      "tests": [
        "FTS duplicate attacks G2-D002: three writes yield one hit in FTS, discover, archive and hybrid keyword search"
      ],
      "executedEvidence": "test-audit/adversarial/fts-direct.f4-final.results.json"
    },
    {
      "id": "ADV-FTS-skip-legacy-healing",
      "defectId": "G2-D002",
      "target": "src/lib/db.ts",
      "fault": "Disable the new legacy duplicate healing call while retaining ordinary schema creation.",
      "tests": [
        "FTS duplicate attacks G2-D002: opening a populated v5 database heals duplicate FTS rows once without losing memories"
      ],
      "executedEvidence": "test-audit/adversarial/fts-direct.f4-final.results.json"
    },
    {
      "id": "ADV-FTS-repeat-healing",
      "defectId": "G2-D002",
      "target": "src/lib/db.ts",
      "fault": "Bypass the migration marker or guard so FTS healing writes run on every open.",
      "tests": [
        "FTS duplicate attacks G2-D002: opening a populated v5 database heals duplicate FTS rows once without losing memories"
      ],
      "executedEvidence": "test-audit/adversarial/fts-direct.f4-final.results.json"
    },
    {
      "id": "ADV-routing-empty-deep-merge",
      "defectId": "DEF-G1-001",
      "target": "src/lib/setup/sections/routing.ts",
      "fault": "Replace reset-specific clearing with the former updateConfig(storePath, { taskModels: {} }) deep-merge call.",
      "tests": [
        "routing reset attacks DEF-G1-001: reset clears several overrides and preserves every unrelated config value",
        "routing reset attacks DEF-G1-001: reset then set one new override does not restore old routing or change unrelated config"
      ]
    },
    {
      "id": "ADV-FTS-skip-recreated-index-population",
      "defectId": "ADV-FTS-001",
      "target": "src/lib/db.ts",
      "fault": "After a future repair, bypass detection/population of a recreated missing FTS table while leaving one-time legacy duplicate marker logic intact.",
      "tests": [
        "FTS duplicate attacks ADV-FTS-001: reopening after external FTS loss keeps the existing memory searchable"
      ],
      "status": "Desired only; defect remains open and no application repair was attempted."
    }
  ]
}
